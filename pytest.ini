[pytest]
testpaths = src/tests
pythonpath = src
addopts = -v --tb=short
markers =
    unit: isolated unit tests for business logic and data access helpers
    london: interaction-based tests with mocks/stubs
    classic: state-based tests without mocks/stubs
    offline: tests expected to pass with network access disabled
